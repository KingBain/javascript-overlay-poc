version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - main-app
    networks:
      - app-network

  main-app:
    build:
      context: .
    expose:
      - "80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge